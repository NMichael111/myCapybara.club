<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{ Feed } Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f8f8;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        img {
            max-width: 300px;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        p {
            margin-bottom: 8px;
            color: #555;
        }

        ul {
            list-style: none;
            padding-left: 0;
        }

        li {
            margin-bottom: 6px;
            color: #777;
        }
    </style>
</head>

<body>
    <h1>{{ pet.name }} Details</h1>
    <img src={{ pet.color }} alt="{{ pet.name }}">

    <h1>Capybara Play Choices</h1>

    <form action="/play" method="post">
        <label for="play">Select Capybara's play:</label><br>

        <input type="radio" id="frontflip" name="play" value="frontflip" required>
        <label for="frontflip">Frontflip</label><br>

        <input type="radio" id="backflip" name="play" value="backflip" required>
        <label for="backflip">Backflip</label><br>

        <input type="radio" id="barrelroll" name="play" value="barrelroll" required>
        <label for="barrelroll">Barrel Roll</label><br>

        <input type="radio" id="walk" name="play" value="walk" required>
        <label for="walk">Walk?</label><br>

        <input type="submit" value="Submit">
    </form>
</body>

</html>